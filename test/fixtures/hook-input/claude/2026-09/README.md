Claude Code hook payloads as documented in September 2026 (hooks reference,
plugins reference). Values are synthetic. The contract test replays every file
here and fails loudly when a field the hooks depend on is missing.
